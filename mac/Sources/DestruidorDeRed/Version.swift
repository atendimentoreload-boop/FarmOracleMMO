import Foundation

/// Versão do app — sincronizada com Windows/Android pela fonte única `/VERSION`.
/// Alterada por `scripts/bump-version.*` (não edite à mão).
enum AppVersion {
    static let current = "1.7.0"
}
